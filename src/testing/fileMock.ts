// Stands in for image imports (`import favicon from '../images/favicon.png'`).
// eslint-disable-next-line import/no-default-export -- jest moduleNameMapper requires a default export
export default 'test-file-stub';
